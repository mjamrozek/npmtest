language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm run build
deploy:
  - provider: npm
    skip_cleanup: true
    email: $EMAIL
    api_key: $API_KEY
    on:
      branch: release
  - provider: pages
    fqdn: npmtestxxx.js.org
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    keep-history: true
    local_dir: docs
    on:
      branch: release